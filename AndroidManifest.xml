<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.odistagon.glone"
	android:versionCode="32" android:versionName="3.2">

	<uses-sdk android:minSdkVersion="4" android:targetSdkVersion="11" />

	<application android:name=".GloneApp"
		android:icon="@drawable/ic_launcher_timestr" android:label="@string/app_name">

		<activity android:label="@string/app_name" android:name=".AyMain">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<activity android:label="@string/app_name" android:name=".AyPrefMain">
		</activity>
		<activity android:label="@string/app_name" android:name=".AyPrefBtns">
		</activity>

		<activity android:label="@string/app_name" android:name=".AyTzSet">
		</activity>

		<receiver
			android:icon="@drawable/ic_launcher_timestr"
			android:label="TimeStripes" android:name="WidgProv" >
		<intent-filter >
			<action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
		</intent-filter>
		<meta-data
			android:name="android.appwidget.provider"
			android:resource="@xml/widget_info" />
		</receiver>

	</application>

</manifest>